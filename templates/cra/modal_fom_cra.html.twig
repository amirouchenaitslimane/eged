{#<!-- Button trigger modal -->#}
{#<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#craModal">#}
    {#Launch demo modal#}
{#</button>#}

{#<!-- Modal -->#}
<div class="modal fade" id="craModal" tabindex="-1" role="dialog" aria-labelledby="craModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="craModalLabel">Cra </h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <img src="{{ asset('images/loader.gif') }}" alt="Loader... " class="loader_cra img-fluid">
               <form id="cra_form" method="post">


            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                <button type="submit" id="save_form" class="btn btn-primary">Enregistrer</button>

            </div>
            </form>
        </div>
    </div>
</div>
<script>
    var route_add_cra = "{{ path('cra_add_ajax') }}";
    var route_remove_cre = "{{ path('cra_remove_ajax') }}";
    var route_update_cra = "{{ path('cra_update_ajax') }}"

</script>