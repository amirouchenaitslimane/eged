{#<!-- Button trigger modal -->#}
{#<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#craModal">#}
    {#Launch demo modal#}
{#</button>#}

{#<!-- Modal -->#}
<div class="modal fade" id="pointModal"  role="dialog" aria-labelledby="pointModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="pointModalLabel">Pointer Un client </h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <img src="{{ asset('images/loader.gif') }}" alt="Loader... " class="loader_cra_save img-fluid">
               <div class="col-md-12">
                   {{ form_start(form, {'attr':{'id':'form_cra','role':'form_cra_add'}}) }}

                   {{ form_row(form.date) }}
                   {{ form_row(form.journee, {'attr':{'class':'select_journee_cra'}}) }}
                   {{ form_row(form.client,{'attr':{'class':'select_client_cra'}}) }}

               </div>

            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Fermer</button>
                <button  id="save_form" class="btn btn-primary">Enregistrer</button>
                {{ form_end(form) }}
            </div>

        </div>
    </div>
</div>
<script>
var route_add_client_cra = '{{ path('add_client_cra_ajax') }}';

</script>